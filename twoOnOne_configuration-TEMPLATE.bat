REM Set path to ImageMagick's convert.exe including convert.exe itself
SET imConvert=
REM Set desired resolution for rasterization
SET imResolution=300x300
REM Set desired output path with trailing backslash
SET outputPath=
REM Set path where this script can place (and overwrite) files without having to check if they exist
SET tempImagePath=